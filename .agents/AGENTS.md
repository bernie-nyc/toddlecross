# Project Rules for Toddlecross

- **Branching Strategy**: 
  - All active development and changes must occur on the `dev` branch.
  - On reaching milestones, the `dev` branch is merged/committed to the `main` branch.
  - After a merge to `main`, the `main` branch is copied/merged back into the `dev` branch to continue work.
